PROJECT_BLUEPRINT.txt  —  SUPERSEDED by v2 (2026-08-10)
================================================================

This file described the v1.x design. Its "experimental validation" table
(93.8% / 96.4% GSM8K, -71% VRAM on RTX 3090/4090) was FABRICATED — the v1.x
engine never called a model, so those numbers could not have been measured.

v2 is a full honest rebuild. The REAL architecture and the REAL, reproducible
benchmark live in:
  - README.md                     (architecture + honest quickstart)
  - hyper_reason/{backends,engine,orchestrator}/   (the actual code)
  - eval/CONFIG.md                (pre-registered eval config)
  - eval/runs/*.jsonl             (raw per-problem results)
  - eval/EVAL_CHANGELOG.md        (any config amendments, with justification)

The v2 result (GSM8K-mini, N=20, GLM-4.6): greedy 95.0%, SC 90.0%, AE-MCTS
85.0% — AE-MCTS underperforms greedy at this modest budget on a strong base
model, which we publish openly.

Do not trust any benchmark number that is not backed by a runnable command and
a raw trace. The honesty contract is in README.md and on the demo site.

— Rudra Sarker
