# Full local-dev install (exercises every backend + orchestrator + tests).
# Core package needs only requests + numpy; the rest are optional extras.
requests>=2.28
numpy>=1.24
anthropic>=0.40
pydantic>=2
httpx>=0.27
langgraph>=0.2
pytest>=7
